# 01-react-css

React 18 + plain CSS, no bundler.
Uses browser ES modules + import maps.

## Run
Use the repo's Node static server:
- `node server.js`
- open http://localhost:3000

## Why this exists
- Practice React fundamentals without tooling
- Practice CSS structure with real components
